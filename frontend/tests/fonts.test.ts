import { describe, it, expect } from 'vitest'

describe('Geist Font (DSGN-02)', () => {
  it.todo('should configure Geist Sans font')
  it.todo('should configure Geist Mono font')
  it.todo('should apply font variables to html element')
})
